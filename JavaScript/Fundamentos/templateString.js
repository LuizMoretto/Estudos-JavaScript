const nome = 'rebeca'
const concatenacao = 'olá' + nome + '!'
const template = `
    olá
    Rebeca!`
console.log(concatenacao, template)

//expressoes...
console.log(`1+1 = ${1+1}`)

const up = texto => texto.toUpperCase() // Utilizado para aumentar a caixa de texto  Foi criado uma constante com a variavel up.
console.log(`ei... ${up('cuidado')}!`)  // sempre está ao meio de (` $ {} `)
                                        // Utilizado para textos grandes

                                    